[package]
name = "brolga-plugin-sdk"
description = "Plugin manifests, capability vocabulary, extension contracts, and the WIT ABI for Brolga"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
categories = ["api-bindings"]
keywords = ["threat-intelligence", "plugin", "wasm", "wit"]

[lints]
workspace = true

[dependencies]
brolga-model = { version = "0.1.0", path = "../brolga-model" }
brolga-security = { version = "0.1.0", path = "../brolga-security" }
schemars = "1.2.2"
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
serde_norway = "0.9.42"
thiserror = "2.0.19"

[dev-dependencies]
serde_json = "1.0.151"
