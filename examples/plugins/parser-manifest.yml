# Example parser plugin manifest — validated by tests and `brolga plugin validate`.
# No capabilities: pure compute. Runnable package: examples/plugins/echo/.
# Host preserves original bytes and transformations; guest emits records only.
schema_version: brolga.plugin.manifest/1.0
name: example.parser.acme-feed
version: 1.0.0
api: "0.1.0"
extension_points:
  - kind: parser
    contract_version: "1.0"
    formats:
      - application/x-acme-feed
      - acme
    outputs:
      - claim
      - sighting
capabilities: []
configuration_schema:
  type: object
  additionalProperties: false
  properties:
    default_tlp:
      type: string
      description: Optional marking hint; host policy still decides.
