# Offline demo fixtures

Nothing here reaches a network. Addresses are TEST-NET-3; domains are reserved for documentation.

- `feed.json` — STIX 2.1 bundle with indicators for an address, a domain, and a SHA-256.
- `rule.yml` — Sigma rule naming the same address and domain.

Both meet on `203.0.113.42` after canonicalisation.
