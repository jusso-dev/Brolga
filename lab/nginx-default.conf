server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    autoindex on;
    charset utf-8;
    # Lab fixtures only — no upload, no CGI.
    location / {
        try_files $uri $uri/ =404;
        add_header X-Brolga-Lab fixtures always;
    }
}
